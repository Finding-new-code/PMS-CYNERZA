---
description: Start the frontend development server and open it in the browser
---

1. Navigate to the frontend directory: `cd pms-frontend`
// turbo
2. Start the development server using npm.cmd: `npm.cmd run dev`
3. Wait for the server to start (usually at http://localhost:3000)
4. Open the browser to `http://localhost:3000`